# ChIP-Atlas Pipeline v2: アップグレード計画

## ステータス概要

| フェーズ | 状態 | 概要 |
|---------|------|------|
| 1. ベンチマーク＆ツール選定 | [x] 完了（Option A） | bwa-mem2 + Parabricks評価済、--nomodel検証済 |
| 2. CWLワークフロー開発（Option A） | [x] 完了 | option-a, option-a-nomodel, option-a-parabricks |
| 2. CWLワークフロー開発（Option B） | [x] 完了 | option-b, option-b-parabricks |
| 3. 二次解析の書き直し | [ ] 未着手 | ターゲット遺伝子、共局在、エンリッチメント |
| 4. 検証（Option A vs v1） | [x] 完了（ce11 + hg38） | ピーク約90%回収、ce11で1.5倍多く検出、hg38で同等 |
| 4. 検証（Option B vs v1） | [x] 完了（ce11） | Option BはAより1.3倍高速、失敗0 |
| 4. 検証（Option A vs B） | [x] 完了（ce11） | 2×2マトリックス（CPU/GPU × A/B） |
| 5. 本番デプロイ | [ ] 未着手 | クラスターセットアップガイド作成済 |
| カスタムCWLランナー | [ ] 未着手 | CWL Zen設計書作成済 |

### 残りのTODO

- [x] ~~Option B「Modern」ワークフロー実装~~ — option-b.cwl + option-b-parabricks.cwl
- [x] ~~Option Bをce11でベンチマーク~~ — 2×2マトリックス完了、Option B推奨
- [x] ~~SRX25595131外れ値の調査~~ — 解決済：マルチラン実験で2つのSRRのうち1つのみダウンロードしていた
- [ ] 残りのゲノム（dm6, mm10, rn6）のベンチマーク — インデックスは構築済
- [ ] hg38でOption Bベンチマーク
- [ ] サンプル選定にインストゥルメントフィルタ追加（PacBio/ONT除外）
- [ ] fast-download.sh（aria2c + ENA/DDBJ）を全ベンチマークスクリプトに統合
- [ ] 二次解析（ターゲット遺伝子、共局在、エンリッチメント）をCWLで書き直し
- [ ] 共有HPCクラスターでのテスト（セットアップガイド作成済）
- [ ] カスタムCWLランナー（CWL Zen）開発
- [ ] 未処理1万件以上のサンプル処理
- [ ] 本番パイプライン選択後に全面再処理

---

## 設計思想

オリジナルのChIP-Atlasパイプラインは意図的にシンプルな設計となっている。各サンプルの実験条件（抗体、細胞種、プロトコル、シーケンス深度）が異なるため、サンプルごとの最適化は大規模処理では現実的ではない。v1パイプラインはすべてのサンプルに対して同一の基本処理を均一に適用しており、このシンプルさは制約ではなく設計上の特長である。

v2では、速度と近代化のトレードオフを評価するため、**2つのパイプラインオプション**を提案する。

### Option A: "Fast Classic" — 同じステップ、高速なツール

v1の処理ロジックをそのまま維持し（同じステップ、可能な限り同じパラメータ）、各ツールを高速な最新版に置き換える。新しいステップは追加しない。

**メリット:**
- v1との結果の比較が最も容易 — 継続性の検証がしやすい
- 新たなバイアス導入のリスクが最小限
- 元の設計思想（均一な基本処理）を尊重
- 実装・保守がシンプル

**デメリット:**
- 品質向上の可能性を見送る（例：アダプタートリミング）
- 実験タイプ別のベストプラクティスを活用できない
- v1データに存在する品質上の問題がそのまま残る

### Option B: "Modern" — ステップの更新とベストプラクティスの適用

パイプライン全体を近代化：QC/トリミングの追加、実験タイプ別のピークコーラー使用、現在のベストプラクティスの適用。

**メリット:**
- サンプルごとの結果品質が向上
- 実験タイプに応じた処理（ChIP-seq vs ATAC-seq vs CUT&Tag）
- 現在のコミュニティ標準に準拠
- CUT&Tagなどの新しい実験タイプに対応しやすい

**デメリット:**
- v1との結果の差異が大きくなる — 継続性の検証が困難
- 実験タイプごとのワークフロー分岐による複雑性の増加
- サンプル間比較に影響するバイアス導入のリスク
- チューニング・保守すべきパラメータが増加

### 評価戦略

1. Option AとOption Bを別々のCWLワークフローとして実装
2. 同一の代表的サンプルセットで両方を実行
3. 結果を比較：A vs v1、B vs v1、A vs B
4. 本番運用でどちらを採用するか決定（または用途に応じて両方使用）

---

## 重要ポリシー: ピークコールにおけるバックグラウンド/インプットコントロール不使用

ChIP-Atlasでは、ピークコールを**バックグラウンドデータ（インプットDNA / ネガティブコントロール）なし**で意図的に実行している。これは見落としではなく、設計上の意思決定である。

- **理由**: 公共リポジトリの40万件以上の実験を対象とする規模では、各ChIPサンプルに対応するインプットコントロールを確実に特定・ペアリングすることは現実的ではない。インプットデータが存在しない、ラベルが誤っている、またはSRAメタデータに明示的に記載されていないケースが多い。
- **仕組み**: MACS2/MACS3をコントロールBAMなしで実行する。代わりにローカルバックグラウンドモデルに対してピークをコールし、ユーザーは3つのq値閾値（1e-05、1e-10、1e-20）を用いて結果のストリンジェンシーをフィルタリングする。
- **これはChIP-Atlasの本質的な特徴** — コントロールの有無にかかわらず、すべての公共データを均一に処理することを可能にしている。
- **v2でもこのポリシーを維持する**: Option AもOption Bも、バックグラウンド/インプットコントロールなしでピークコールを行うこと。コントロールペアリングのロジックは実装しない。

---

## 目標

1. **処理時間の大幅短縮**（現在 約1日/サンプル）
2. **ツールの近代化** — 10年前のバージョンを最新に置き換え
3. **GPU高速化** — 有効な箇所で活用
4. **CWLベースのオーケストレーション** — ポータブル、再現可能、ランナー非依存
5. **カスタムCWLランナー** — 最小限、最速、あらゆるインフラに柔軟対応
6. **全範囲** — 一次処理と二次解析の両方を書き直し
7. **新しい実験タイプのサポート** — ChIP-seq/DNase-seq/ATAC-seq/Bisulfite-seqに加えCUT&Tag
8. **v1との検証** — ピーク数とオーバーラップを比較し差異を把握

## インフラストラクチャ

### ベンチマークマシン（このワークステーション）

| コンポーネント | スペック |
|-------------|--------|
| CPU | Intel Xeon Gold 6226R @ 2.90GHz |
| CPUコア数 | 32（16物理 × 2 HT） |
| RAM | 93 GB |
| GPU | NVIDIA RTX 6000 Ada Generation（48 GB VRAM） |
| ストレージ | 3.6 TB NVMe（ローカル）+ 3.6 TB HDD ×2（/data2, /data3） |

本ドキュメントのすべてのベンチマーク計測値はこのマシンで取得。

### 本番運用目標環境: 遺伝研スーパーコンピュータシステム 2025

出典: https://sc.ddbj.nig.ac.jp/en/guides/hardware/hardware2025/

| ノードタイプ | 台数 | CPU | コア数/ノード | RAM/ノード | GPU |
|------------|------|-----|-------------|----------|-----|
| Type 1（CPU） | 50 | AMD EPYC 9654（96c ×2） | 192 | 1.5 TB | — |
| Type 2（CPU） | 28 | AMD EPYC 7702（64c ×2） | 128 | 512 GB | — |
| Type 2（GPU） | 3 | AMD EPYC 9334（32c ×2） | 64 | 768 GB | 8× NVIDIA L40S |
| Type 3（アクセラレータ） | 2 | AMD EPYC 7713P（64c） | 64 | 2 TB | 4× PEZY-SC3 |

- **総CPUコア数**: 13,184
- **ストレージ**: 13.3 PB Lustre
- **ネットワーク**: InfiniBand HDR100（100 Gbps）、GPUノードは400 Gbps
- **コンテナ**: Singularity/Apptainer利用可能

### 遺伝研スパコンでの推定高速化

| 要因 | ベンチマークマシン | 遺伝研 Type 1ノード | 推定高速化 |
|------|-----------------|-------------------|-----------|
| CPUコア（bwa-mem2） | 32コア @ 2.9 GHz | 192コア @ 2.4 GHz | ノードあたり約3-4倍 |
| RAM | 93 GB | 1.5 TB | 遺伝研ではボトルネックなし |
| 並列ノード数 | 1 | 50（Type 1）+ 28（Type 2） | 78サンプル並列処理 |
| GPU | 1× RTX 6000 Ada | 8× L40S/ノード（3ノード） | GPUノードあたり約8倍、計24 GPU |

**遺伝研での40万hg38サンプル処理所要時間見積もり**（Option B CPU）:

| シナリオ | サンプルあたり | 並列数 | 合計時間 |
|---------|-------------|--------|---------|
| ベンチマークマシン（1ノード） | 17分（ce11）/ 61分（hg38） | 1 | 約46年 |
| 遺伝研 Type 1（1ノード、192コア） | 約15-20分（hg38推定） | 1 | 約11-15年 |
| 遺伝研 Type 1（50ノード） | 約15-20分 | 50 | 約80-110日 |
| 遺伝研 Type 1+2（78ノード） | 約15-20分 | 78 | 約50-70日 |
| 遺伝研 GPU（3ノード × 8 L40S） | 約5-10分（推定） | 24 | 約12-28日 |

注: ダウンロード時間は含まず。遺伝研の高帯域ネットワーク（100 Gbps InfiniBand + NII/SINETの高速外部接続）により、ダウンロードのオーバーヘッドはベンチマークマシンより大幅に削減される見込み。

- **クラウド**: 必要に応じて利用可能（AWS/GCP）
- **コンテナ**: Singularity/Apptainer

## Phase 1: ベンチマークとツール選定 [x]

### 1.1 ~~v1パイプラインのプロファイリング~~（スキップ）

v1の挙動を再現することは現実的ではない — 元のシェルスクリプトでは多くの変数やファイルパスがハードコードまたは暗黙的に宣言されている。代わりに以下の方針とする：

- 新しいパイプライン（Option AおよびB）を直接構築
- 新パイプラインでステップごとの処理時間をベンチマーク
- chip-atlas.dbcls.jpに既に公開されているv1の既存結果と出力を比較
- 新パイプラインはいずれにせよ高速になるため、v1のプロファイリングは労力に見合わない

### 1.2 アライナー評価

同一サンプルセットで3つのアライナーを実行し、速度・メモリ・アライメント品質を比較：

| アライナー | タイプ | 備考 |
|-----------|--------|------|
| bwa-mem2 | CPU (AVX-512) | 最速のCPUアライナー、BWAのドロップイン置換 |
| minimap2 | CPU | 汎用性が高く高速、ロングリード対応で将来性あり |
| Parabricks (GPU BWA-MEM) | GPU (NVIDIA) | 極めて高速、商用ライセンスが必要 |

比較指標：
- サンプルあたりの実時間
- ピークメモリ使用量
- v1のBowtie2ベースラインとのマッピング率比較
- 下流のピークコール結果の一貫性

### 1.3 ツールマッピング: Option A vs Option B

| ステップ | v1ツール | Option A (Fast Classic) | Option B (Modern) |
|---------|---------|------------------------|-------------------|
| SRAダウンロード | SRA Toolkit 2.3.2-4 | SRA Toolkit最新版 (fasterq-dump) | Aと同じ |
| FASTQ QC | (なし) | (なし) | fastp (QC+トリミングを一括処理) |
| トリミング | (なし) | (なし) | fastp |
| アライメント | Bowtie2 2.2.2 | bwa-mem2 / minimap2 / Parabricks | Aと同じ |
| BAM処理 | SAMtools 0.1.19 | SAMtools最新版 (1.20+) | Aと同じ |
| 重複除去 | samtools rmdup | samtools markdup | Aと同じ |
| カバレッジトラック | bedtools 2.17.0 + bedGraphToBigWig | bedtools最新版 + bedGraphToBigWig | deeptools bamCoverage (BAM→BigWig直接変換) |
| ピークコール | MACS2 2.1.0 (全タイプ共通) | MACS3 (全タイプ共通) | MACS3 (ChIP-seq), HMMRATAC (ATAC-seq), SEACR (CUT&Tag) |
| フォーマット変換 | UCSC bedToBigBed | UCSC tools最新版 | Aと同じ |

**主な違い**: Option Aはv1と同じステップを維持（QCなし、トリミングなし、全タイプ共通のピークコーラー）。Option BはQC/トリミングを追加し、実験タイプ別のピークコーラーを使用。

### 1.4 CUT&Tagに関する考慮事項（Option Bのみ）

- CUT&TagはChIP-seqよりバックグラウンドが低い — SEACRが推奨ピークコーラー
- CWLワークフロー内でピークコールの分岐が必要
- アライメントパラメータが異なる可能性あり（例：フラグメントサイズの期待値）

## Phase 2: CWLワークフロー開発 [x] Option A / [x] Option B

### 2.1 ワークフロー構成

```
chip-atlas-pipeline-v2/
├── cwl/
│   ├── tools/                        # CWL CommandLineTool定義（ツールごとに1つ）
│   │   ├── fasterq-dump.cwl
│   │   ├── bwa-mem2.cwl
│   │   ├── fastp.cwl                 # Option Bのみ
│   │   ├── samtools-sort.cwl
│   │   ├── samtools-markdup.cwl
│   │   ├── macs3-callpeak.cwl
│   │   ├── deeptools-bamcoverage.cwl  # Option Bのみ
│   │   ├── bedtools-genomecov.cwl     # Option Aのみ
│   │   ├── bedgraphtobigwig.cwl
│   │   ├── bedtobigbed.cwl
│   │   └── ...
│   ├── workflows/
│   │   ├── option-a.cwl              # Fast Classic: v1と同じステップ、最新ツール
│   │   ├── option-b.cwl              # Modern: QC+トリミング+タイプ別コーラー
│   │   ├── target-genes.cwl          # 二次解析: ピーク-TSSオーバーラップ
│   │   ├── colocalization.cwl        # 二次解析: 共結合解析
│   │   ├── enrichment.cwl            # 二次解析: in silico ChIP
│   │   └── full-pipeline.cwl         # トップレベル: 一次処理+二次解析
│   └── inputs/
│       ├── hg38.yml                  # ゲノムごとの入力テンプレート
│       ├── mm10.yml
│       └── ...
├── containers/
│   ├── Singularity.bwa-mem2
│   ├── Singularity.macs3
│   ├── Singularity.fastp
│   └── ...
├── scripts/
│   ├── batch-submit.sh               # 複数サンプルの一括投入
│   ├── metadata-filter.py            # SRAメタデータフィルタリング（シェルロジックの置換）
│   └── validate-vs-v1.py             # v1との一貫性比較ツール
├── docs/
│   ├── current-pipeline.md
│   └── v2-plan.md
└── tests/
    ├── test-samples.yml               # テスト用代表サンプルセット
    └── expected/                      # CI用の期待出力
```

### 2.2 CWL設計原則

- **CWL v1.2**仕様
- ツールごとに1つの`CommandLineTool` — 粒度が細かく、再利用可能、テスト可能
- `Workflow`ドキュメントでツールをパイプラインに構成
- `scatter`によるサンプル間の並列実行
- すべてのツールをSingularityコンテナでラップ
- 入出力の型を厳密に定義しバリデーション
- 初期テストは**cwltool**で実施
- Option AとOption Bは同じツール定義を共有し、ワークフローレベルでのみ異なる

### 2.3 カスタムCWLランナー（サブプロジェクト）

- 最小限の実装 — このパイプラインに必要なCWL v1.2サブセットをサポート
- SLURM/SGE/ローカルへの直接ジョブ投入（中間レイヤーなし）
- Singularityネイティブ（Docker変換レイヤーなし）
- 依存関係解決付きの並列ステップ実行
- 大規模処理向け設計（40万サンプル以上）
- 言語は未定（Rust? Go? Python?）

## Phase 3: 二次解析の書き直し [ ]

### 3.1 ターゲット遺伝子解析

- シェル+bedtoolsをCWLワークフローに置き換え
- bedtools最新版でピーク-TSSオーバーラップを計算
- STRING連携をPythonで書き直し
- 出力: TSVテーブル

### 3.2 共局在解析

- カスタムJavaツール（`coloCA.jar`）をPython実装に置き換え
- 同じアルゴリズム: ガウス分布フィット → Zスコアグループ → ペアワイズスコアリング
- STRINGスコアを統合
- CWL CommandLineToolとしてラップ

### 3.3 エンリッチメント / In Silico ChIP

- PythonとCWLラッパーで書き直し
- bedtools intersect + scipyによるFisherの正確検定
- statsmodelsによるBH補正

## Phase 4: 検証 [x] Option A / [x] Option B

### 4.1 サンプル選定

データソース: `https://chip-atlas.dbcls.jp/data/metadata/experimentList.tab`

#### 層別化の次元

1. **ゲノム**（現行の6アセンブリ、レガシーアセンブリはスキップ）:
   - hg38, mm10, rn6, dm6, ce11, sacCer3

2. **実験タイプ**（6種類）:
   - Histone, TFs and others, ATAC-Seq, DNase-seq, RNA polymerase, Bisulfite-Seq
   - スキップ: Input control, Unclassified, No description, Annotation tracks

3. **リード数階層**（カラム8の最初のカンマ区切り値に基づく）:
   - Low: 1000万リード未満
   - Medium: 1000万〜5000万リード
   - High: 5000万リード超

#### 選定方法

- **ゲノム × 実験タイプ × リード数階層**の各組み合わせについて:
  - アクセッション番号の降順でソート（新しい実験を優先 — 最新のシーケンス機器にバイアス）
  - 上位から**3サンプル**を選定
- 生物種ごとに複数サンプルを確保し、ゲノム構造に起因するマッパー/ピークコーラーの挙動の違いを捕捉
- **想定サイズ**: 最大約270サンプル（6ゲノム × 6タイプ × 3階層 × 3サンプル）、組み合わせが少ない箇所ではそれより少なくなる

#### 選定スクリプト

`scripts/select-validation-samples.py` — experimentList.tabを読み込み、フィルタリングと層別化を適用し、選定サンプルリストを`data/validation-samples.tsv`に出力

### 4.2 三者間比較

| 比較 | 目的 |
|------|------|
| Option A vs v1 | ツールのアップグレードのみで結果が壊れないことを検証 |
| Option B vs v1 | QC/トリミング/タイプ別コーラー追加の影響を把握 |
| Option A vs Option B | 近代化ステップの効果を分離 |

指標：
- **ピーク数**: 各q値閾値でのピーク数
- **ピークオーバーラップ**: Jaccard係数とオーバーラップ係数
- **探索的分析**: 合否閾値を設定する前に差異を可視化
- オプション: BigWigシグナル相関（Pearson/Spearman）は後の段階で

### 4.3 検証ツール

- `validate-vs-v1.py`: 2つのBEDファイルを入力し、ピーク数・オーバーラップ統計を報告、比較プロットを生成
- テストスイートの一部として実行

## 進捗ログ

### 2026-03-20: パイプライン初期実装とテスト

**完了項目:**

1. **CWLツール定義**（11ツール）:
   - `fasterq-dump.cwl` — SRAダウンロード (sra-tools 3.0.10)
   - `bwa-mem2-align.cwl` — アライメント (bwa-mem2 2.2.1)
   - `samtools-sort.cwl` — name/coordinateソート (samtools 1.19.2)
   - `samtools-fixmate.cwl` — markdup用のmate scoreタグ追加
   - `samtools-markdup.cwl` — 重複除去（非推奨のrmdupを置換）
   - `samtools-mapped-count.cwl` — RPM正規化用のリード数カウント
   - `bedtools-genomecov.cwl` — BedGraphカバレッジ (bedtools 2.31.1)
   - `bedgraphtobigwig.cwl` — BedGraph → BigWig (UCSC tools 482)
   - `macs3-callpeak.cwl` — コントロールなしのピークコール (MACS3 3.0.4)
   - `bedtobigbed.cwl` — BED → BigBed (UCSC tools 482)
   - `parabricks-fq2bam.cwl` — GPU高速化アライメント+ソート+重複除去 (Parabricks 4.3.1)

2. **2つのワークフローバリアント**:
   - `option-a.cwl` — CPUパイプライン（bwa-mem2 → sort → fixmate → sort → markdup → ...）
   - `option-a-parabricks.cwl` — GPUパイプライン（fq2bamがalign+sort+markdupを置換）

3. **検証用サンプルセット**: 6ゲノム × 6実験タイプ × 3リード数階層で301サンプルを選定

4. **初回テスト実行** sacCer3 (SRX22049197, H3K4me3, 約95万リード):

| 閾値 | v1 (MACS2 + Bowtie2) | v2 bwa-mem2 | v2 Parabricks |
|------|---------------------|-------------|---------------|
| q 1e-05 | 429             | 539         | 539           |
| q 1e-10 | —               | 396         | 397           |
| q 1e-20 | —               | 286         | 286           |

- bwa-mem2とParabricksはほぼ同一の結果（±1ピーク）
- v2はq 1e-05でv1より約25%多くのピークを検出（bwa-mem2がBowtie2より高感度なため想定通り）

**テスト中に発見・修正した問題:**
- `samtools markdup`は事前に`fixmate -m`が必要 → name-sort → fixmate → coord-sortフローを追加
- CWLの`float`型が極小のq値（1e-10、1e-20）を0に丸める → `string`型に変更
- 複数のBiocontainers Dockerイメージタグが存在しない → 全タグを検証・修正
- Parabricksはリードグループに`PU`と`LB`フィールドが必須 → fq2bamツールに追加

### 2026-03-25: ce11フルベンチマーク完了（Option A「Fast Classic」）

**以下のベンチマークはすべてOption A「Fast Classic」** — v1と同じ処理ステップ（QCなし、トリミングなし、全タイプ共通のピークコーラー）で、ツールのみ最新版に置換。Option B「Modern」（fastp、実験タイプ別コーラー）は未テスト。

**ベンチマーク範囲**: ce11サンプル46件（6実験タイプ × 3リード数階層 × 約3サンプル）、PacBioサンプル1件は除外。

#### テストした3つのパイプライン

| パイプライン | ワークフロー | 説明 |
|------------|-------------|------|
| CPU（デフォルト） | `option-a.cwl` | bwa-mem2、MACS3モデル構築あり |
| CPU（nomodel） | `option-a-nomodel.cwl` | bwa-mem2、MACS3 `--nomodel --extsize 200` |
| GPU（Parabricks） | `option-a-parabricks.cwl` | Parabricks fq2bam、MACS3 `--nomodel` |

#### 成功率

| パイプライン | 成功 | 失敗 | 備考 |
|------------|------|------|------|
| CPU（デフォルト） | 34 | 12 | MACS3モデル構築失敗6件、SE命名4件、ソートglob 1件、PacBio 1件 |
| CPU（nomodel） | 45 | 1 | PacBioサンプルのみ失敗 |
| GPU（Parabricks） | 45 | 0 | SE修正（`--in-se-fq`）後すべて成功 |

**結論**: `--nomodel --extsize 200`はMACS3のモデル構築失敗をすべて解消し、精度への影響はない。

#### 処理時間（パイプラインのみ、ダウンロード除く）

| リード数階層 | サンプル数 | CPU（nomodel） | GPU（Parabricks） | 高速化率 |
|------------|-----------|---------------|-------------------|---------|
| Low（<1000万） | 15 | 7分 | 5分 | 1.25倍 |
| Medium（1000万-5000万） | 15 | 16分 | 11分 | 1.47倍 |
| High（>5000万） | 15 | 43分 | 31分 | 1.37倍 |
| **全体** | **45** | **22分** | **16分** | **1.37倍** |

v1の約1日/サンプルと比較して、v2パイプラインはCPUで**約65倍**、GPUで**約90倍**高速（ce11データ）。

#### ピーク数比較

**nomodel vs デフォルトモデル（CPU、q 1e-05）**:
- ATAC-Seq、DNase-seq、Histone、TFs: **ピーク数完全一致**（差異0）
- RNA polymerase: **2%未満の差異**（推定vs固定フラグメントサイズによる軽微な差）
- `--nomodel`をデフォルトとして安全に使用可能

**CPU vs GPU（q 1e-05）**:
- 45サンプルを比較
- **総ピーク数差異0.9%**（171,677 vs 173,185）
- ほとんどのサンプルで±1-15ピークの差異
- bwa-mem2とParabricks（BWA-MEM）は実質的に同一の結果

#### ダウンロード速度

| 方法 | 速度 | 備考 |
|------|------|------|
| fasterq-dump（NCBI） | ベースライン | シングルスレッドSRA変換 |
| aria2c + ENA | **2.5倍高速** | 8並列HTTP接続、生成済みFASTQ |
| aria2c + DDBJ | DRRで利用可能 | キャッシュ済みfastqlistでパス検索 |

アクセッション番号プレフィックスによるダウンロードルーティング: DRR → DDBJ → ENA → fasterq-dump、SRR/ERR → ENA → fasterq-dump。

#### 発見・修正した問題

| 問題 | 修正 |
|------|------|
| MACS3モデル構築が低シグナルサンプルで失敗 | `--nomodel --extsize 200`を追加 |
| シングルエンドFASTQ命名（`SRR.fastq` vs `SRR_1.fastq`） | スクリプトで柔軟なFASTQ検出 |
| ParabricksのSEリードに`--in-se-fq`が必要 | CWLツールで条件分岐 |
| ENA `_subreads.fastq`命名（PacBioデータ） | 柔軟なFASTQ検出 + PacBioを検証から除外 |
| CWL `float`型が極小q値を切り捨て | `string`型に変更 |
| MACS3 `xls`出力がピークなし時に欠落 | 出力をオプション化（`File?`） |

#### データ品質に関する発見

- SRX2170085（ce11、Bisulfite-Seq）は**PacBio RS II**サンプルで、SRAメタデータでの分類が不適切。ショートリードインデックスに対するマッピング率は4.5%。v1パイプラインでもインストゥルメントモデルによるフィルタリングが必要だった。**推奨事項**: v2のサンプル選定にインストゥルメントフィルタを追加し、PacBio/ONTサンプルを除外する。

### 2026-03-27: hg38ベンチマーク完了（Option A「Fast Classic」）

**ベンチマーク範囲**: hg38サンプル18件（すべてOption A「Fast Classic」+ `--nomodel`を使用）（実験タイプ × リード数階層ごとに1件）、CPUとGPUパイプラインを並列実行。

#### 処理時間

| リード数階層 | サンプル数 | CPU（nomodel） | GPU（Parabricks） | 高速化率 |
|------------|-----------|---------------|-------------------|---------|
| Low（<1000万） | 6 | 7分 | 5分 | 1.4倍 |
| Medium（1000万-5000万） | 6 | 60分 | 31分 | 1.9倍 |
| High（>5000万） | 6 | 118分 | 72分 | 1.6倍 |
| **全体** | **18** | **61分** | **36分** | **1.7倍** |

hg38でのGPU高速化率（1.7倍）はce11（1.37倍）より大きい — GPU加速は大きなゲノムでより効果的。

特筆すべき結果:
- Bisulfite-Seq 4500万リード: CPU 91分 vs GPU **8分**（10.2倍高速化）
- Bisulfite-Seq 3.14億リード: CPU 214分 vs GPU **67分**（3.1倍高速化）
- 一部のサンプルでCPUがGPUより高速（ATAC-Seq 5900万、Histone 1.23億） — 両ベンチマーク並列実行によるリソース競合の可能性

#### ピーク数比較（CPU vs GPU、q 1e-05）

- 14サンプルを比較（GPU出力1件欠損）
- **総ピーク数差異0.8%**（168,493 CPU vs 167,069 GPU）
- ほとんどのサンプルで1%未満の差異、ce11の結果と一致

#### ChIP-Atlas全データ再処理の所要時間見積もり（hg38）

パイプライン平均処理時間に基づく見積もり（ダウンロード除く）:

| シナリオ | サンプルあたり | hg38 20万サンプル | 備考 |
|---------|-------------|------------------|------|
| CPU（1ノード、8コア） | 61分 | 約23年 | 非現実的 |
| GPU（1ノード、1 GPU） | 36分 | 約14年 | 非現実的 |
| CPUクラスター（10ノード） | 61分 | 約2.3年 | |
| GPUクラスター（10 GPU） | 36分 | 約1.4年 | |
| CPUクラスター（100ノード） | 61分 | 約84日 | 現実的 |

**結論**: 大規模処理には大幅な並列化が必要。100+ノードのCPUクラスターまたは10+GPUノードが現実的な再処理期間に必要。

#### ce11 vs hg38 スケーリング比較

| 指標 | ce11（100MBゲノム） | hg38（3GBゲノム） | 比率 |
|------|--------------------|--------------------|------|
| CPU平均パイプライン | 22分 | 61分 | 2.8倍 |
| GPU平均パイプライン | 16分 | 36分 | 2.3倍 |
| GPU高速化率 | 1.37倍 | 1.7倍 | 大きなゲノムでGPUがより効果的 |

### 2026-03-27: v1 vs v2 ピークオーバーラップ解析（Option A「Fast Classic」）

chip-atlas.dbcls.jpからv1のBEDファイルをダウンロードし、bedtools intersectでv2 Option A結果とのピークオーバーラップを比較。

#### ce11（v1ピークあり35サンプル + v1=0が10サンプル）

| 指標 | 値 |
|------|-----|
| v1ピークのv2での回収率 | 平均**約90%** |
| v2がより多くのピークを検出 | 77%のサンプル（総ピーク数1.6倍） |
| v1=0だがv2でピーク検出 | 10サンプル（各数千ピーク） |

**実験タイプ別オーバーラップ:**

| タイプ | 平均オーバーラップ | 備考 |
|--------|-------------------|------|
| RNA polymerase | 97% | 非常に高い一致 |
| Histone | 93% | 良好、v2がより多く検出 |
| ATAC-Seq | 88% | 高リード数サンプルでv2が2-40倍多く検出 |
| TFs and others | 87% | 良好な一致、類似したピーク数 |
| DNase-seq | 87% | v2がより多く検出 |

#### hg38（v1ピークあり12サンプル）

| 指標 | 値 |
|------|-----|
| v1ピークのv2での回収率 | 平均**約77%** |
| 90%以上のオーバーラップ | 4/12サンプル（DNase-seq、ATAC-Seq） |

#### マルチ閾値比較（q 1e-05、1e-10、1e-20）

**v1 vs v2 CPUピーク数比率（全閾値）:**

| ゲノム | q05 (v2/v1) | q10 (v2/v1) | q20 (v2/v1) |
|--------|------------|------------|------------|
| ce11（35サンプル） | 1.5倍 | 1.5倍 | 1.7倍 |
| hg38（11サンプル、外れ値除外） | 1.0倍 | 0.9倍 | 0.8倍 |

- ce11: v2は**全閾値で一貫してより多くのピークを検出**、厳しい閾値ほど比率が増加 — v2の追加ピークは高信頼度
- hg38: v2とv1はq05で**ほぼ同等**、厳しい閾値ではv2がやや少ない
- ゲノム間の差異はアライナーのゲノム構造への依存性を反映している可能性（ce11 ~100MB vs hg38 ~3GB）

**v2 CPU vs GPU 閾値間の一貫性:**

| 閾値 | ce11（45サンプル） | hg38（18サンプル） |
|------|-------------------|-------------------|
| q05 | 0.8%差 | 0.8%差 |
| q10 | 1.1%差 | — |
| q20 | 1.3%差 | — |

CPUとGPUは全閾値でほぼ同一の結果 — アライナーの選択（bwa-mem2 vs Parabricks BWA-MEM）がピークコールへの影響は最小限。

#### 解釈

1. **v2はv1ピークの大部分を回収**（ce11で約90%、hg38で約77%） — コアシグナルは保存
2. **v2はce11でより多くのピークを検出**（1.5倍）だが、**hg38ではほぼ同等** — v1-v2差異はゲノム依存
3. **v1=0のサンプルがv2でピーク検出** — これは退行ではなく改善（ce11で10サンプル）
4. **一部サンプルでv2のピークが少ない** — ツール差異により想定内；q値閾値で調整可能
5. **オーバーラップは実験タイプにより変動** — RNA polymeraseが最も高い一致、ATAC-Seqで最も新規ピークが多い
6. **CPUとGPUは互換性あり** — 全閾値で1.5%未満のピーク差異

**外れ値（解決済）**: ~~SRX25595131（Histone、hg38、SE）でv1=8,797 → v2=201~~ この実験は**2つのSRRラン**（SRR30125615: 300万リード + SRR30125616: 700万リード）を持つが、ベンチマークでは最初のランのみダウンロード（データの30%）。両ラン結合後: **v2=6,633ピーク** — v1の8,797と整合（ツール差異による約25%の差、バグではない）。**修正**: `download-experiment.sh`で実験ごとに全ランを解決しFASTQを結合、v1の挙動と一致。

---

### 2026-03-28: 2×2ベンチマーク完了（ce11）

4つのパイプラインバリアントすべてを46 ce11サンプルでベンチマーク。

#### パイプライン処理時間（平均、パイプラインステップのみ）

|  | トリミングなし（Option A） | fastpトリミング（Option B） |
|--|--------------------------|---------------------------|
| **CPU（bwa-mem2）** | 22分 | 17分 |
| **GPU（Parabricks）** | 16分 | **13分** |

#### リード数階層別

| 階層 | A CPU | A GPU | B CPU | B GPU |
|------|-------|-------|-------|-------|
| Low（<1000万） | 7分 | 5分 | 6分 | 4分 |
| Medium（1000万-5000万） | 16分 | 11分 | 12分 | 9分 |
| High（>5000万） | 43分 | 31分 | 33分 | 27分 |

#### 成功率

| パイプライン | 成功 | 失敗 | 備考 |
|------------|------|------|------|
| Option A CPU | 45 | 1 | PacBioサンプル失敗 |
| Option A GPU | 45 | 0 | |
| Option B CPU | **46** | **0** | fastpがPacBioリードを100%フィルタ → パイプライン成功 |
| Option B GPU | **46** | **0** | |

#### 主要な知見

1. **Option BはOption Aより高速**（CPU: 17分 vs 22分、GPU: 13分 vs 16分） — fastpでリード数が若干減少し、deeptools bamCoverageがbedtools genomecov + bedGraphToBigWigより高速
2. **GPUはさらに約1.3倍の高速化**
3. **Option B + GPUが最速**（13分平均） — Option A CPUの1.7倍高速
4. **Option Bはより堅牢** — fastpが品質ゲートとして機能、PacBioリードを100%フィルタし失敗を防止
5. **ピーク数は4バリアント間で類似**（<5%差異）
6. v1の約1日/サンプルと比較して、最も遅いバリアント（Option A CPU、22分）でも**約65倍高速**

#### 推奨

**Option B + GPU**を本番運用に推奨（GPU利用可能な場合）。**Option B CPU**をCPUのみのクラスターに推奨。Option Aは非推奨 — Option Bの方が高速かつ堅牢で、デメリットなし。

---

## Phase 5: 本番デプロイとマイグレーション [ ]

### 5.1 判断ポイント

Phase 4の検証後に決定：
- ~~**Option Aを採用** — v1との一貫性が最重要の場合~~
- **Option Bを採用** — 高速かつ堅牢、品質への影響は軽微（推奨）
- **Option B + GPU** — GPU利用可能な場合の最速オプション

### 5.2 段階的ロールアウト

1. 未処理の1万サンプル以上を選択したオプションで処理
2. 既存40万サンプルのサブセットを再処理して検証
3. 結果が満足できれば全面的に再処理

### 5.3 インフラ更新

- メタデータフィルタリング: Pythonで書き直し（シェルスクリプトを置換）
- 増分更新ロジック: 新しいSRAアクセッションを検出しキューに追加
- データ配信: 後方互換性のため同じURL構造を維持

## タイムライン（大まかなフェーズ、時間見積もりではない）

1. [x] **ベンチマーク** — アライナー評価（bwa-mem2、Parabricks）、--nomodel検証
2. [x] **CWL開発（Option A）** — option-a、option-a-nomodel、option-a-parabricks
3. [x] **CWL開発（Option B）** — option-b、option-b-parabricks（fastp + deeptools）
4. [ ] **カスタムランナー** — CWL Zen設計書作成済、実装は未着手
5. [ ] **二次解析** — PythonとCWLで書き直し
6. [x] **検証（Option A）** — A vs v1（ce11 + hg38）
7. [x] **検証（Option B）** — 2×2マトリックス完了（ce11）、Option B推奨
8. [ ] **判断と本番運用** — Option B推奨、クラスターでの検証が次ステップ

## 未解決事項

- [ ] HPCクラスターのジョブスケジューラは？（SLURMを想定）
- [ ] Parabricksのライセンス — 利用可能か、取得が必要か？
- [ ] CUT&Tag固有のパラメータとピークコーラー（SEACR vs MACS3）
- [ ] v2出力のデータストレージ戦略（同じファイルシステム？新しい構成？）
- [ ] hg38でv1と同等だがce11で1.5倍多くピーク検出される理由
- [ ] hg38でのOption Bベンチマーク
- [ ] 残りのゲノム（dm6、mm10、rn6）でのベンチマーク
- [x] ~~SRX25595131外れ値~~ — 解決済：マルチランダウンロードの問題（進捗ログ参照）
- [x] ~~カスタムCWLランナーの言語選択~~ — Rust（CWL Zen設計書参照）
